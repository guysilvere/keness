/**
 * Sync engine — orchestrates create / push / sync / rm operations.
 * Implemented in ROADMAP step 4 (MVP features).
 */

export type SyncAction = 'push' | 'sync' | 'rm';

// TODO(step-4): implement push, diff, sync, rm with preview + confirmation
export const syncEngine = {
  placeholder: true,
} as const;
